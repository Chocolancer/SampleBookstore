@model HartleyMarksAssignment.Models.Category

<div class="col-md-12 category-box">
    <h1>@Model.Title</h1>
    <div class="well row">
        @foreach (var item in @Model.Items)
        {
            @Html.Partial("_Item", item);
        }
    </div>
</div>
